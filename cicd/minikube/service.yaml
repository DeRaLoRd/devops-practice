apiVersion: v1
kind: Service
metadata:
  name: sentrim-service
spec:
  selector:
    app: sentrim
  type: NodePort
  ports:
    - port: 8080
      targetPort: 80
      #nodePort: 30080
      # leave blank for now idc fam
